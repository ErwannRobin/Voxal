# Voxal benchmark results

What a Voxal room actually costs — bandwidth, CPU, memory and the delays you
feel — measured by the harness in `tests/bench/` and published here so the
numbers can be read without running anything.

- **[Open the dashboard](https://erwannrobin.github.io/Voxal/benchmark.html)** —
  the same run as charts, with every chart's table twin (source:
  [`docs/benchmark.html`](benchmark.html), regenerated by the same command as
  this page).
- **[How it is measured, and what the numbers are not](benchmarking.md)** — read
  this before quoting anything below.
- **`docs/bench-history.ndjson`** — one line per published run, so the numbers
  can be followed across versions rather than only read at one. It appears with
  the first `make bench-publish` after this harness landed; `make bench-history`
  prints it as a table, and the *Across versions* section below is drawn from
  it.
- **[Why audio is a mesh and video may not be](video-routing.md)** — the design
  decision most of these numbers are a consequence of.

## Before you quote a number

Three limits apply to everything on this page, and they are properties of the
harness, not small print:

1. **Every peer of a room runs in one browser on one machine, over loopback.**
   Latency is therefore a *floor* — it says the code adds nothing silly, and
   nothing about a real network.
2. **CPU and memory are whole-room totals.** One browser serves every peer, so
   the figure is the cost of running an N-person room on one machine. It is not
   per-peer, and dividing it by N gives a number that means nothing.
3. **Nothing here asserts a threshold.** The suite checks only that the call was
   working, so a zero means "this cost nothing", never "this never connected".

The run below carries the machine it was measured on and the network label it
was measured under. If those look nothing like yours, the shape of the curves
will still hold and the absolute figures will not.

## How this page is kept current

It is republished by hand, from a real run:

```sh
make bench            # measure (minutes, one worker, no thresholds)
make bench-publish    # write the tables here and the dashboard beside them
```

`make bench-publish` overwrites everything between the markers below, rewrites
`docs/benchmark.html` and appends one summary line to `bench-history.ndjson`;
then all three files are committed. Nothing in CI touches them — a benchmark measured on a shared CI runner would publish the
runner's noise as the product's performance, and `main` takes no direct push in
any case (see [required checks](required-checks.md)).

So the freshness of this page is visible in it: the date, machine and run count
in the block below are the run it was published from. If they predate a release
that changed the media path, re-run the two commands above.

## Results

<!-- bench-results -->
<!-- Generated by `make bench-publish` from a real run. Do not edit by hand:
     the next publish overwrites everything between these markers. -->

Run `full-1` — 18 scenario runs, 2026-09-18T14:07:24.193Z (the raw measurements are not committed; see `docs/benchmarking.md`)

**Machine**: linux/x64, 4 cores (Intel(R) Xeon(R) Processor @ 2.80GHz), 16096 MiB RAM, node v22.22.2
**Network**: `unshaped-loopback`

> Every peer of a room runs in one Chromium on one machine over loopback. Latency is therefore a **floor**, not a field measurement, and the CPU/RSS columns are the cost of the **whole room** on that one machine — they are not per-peer and must not be divided by the peer count. See `docs/benchmarking.md`.

## Mesh scaling

Audio is full mesh, always. A speaker uploads one Opus stream **per other peer**, so the upload column is the number that decides how large a room can get on a domestic uplink. An SFU-based product holds this column flat.

| Peers | Speakers | Speaker ↑ | Listener ↑ | Listener ↓ | RTT p50 | Loss p50 | Room CPU | Room RSS |
|---|---|---|---|---|---|---|---|---|
| 2 | 1 | 48 kb/s | 35 kb/s | 48 kb/s | 1 ms | 0.0% | 30% | 1085 MiB |
| 3 | 1 | 97 kb/s | 86 kb/s | 100 kb/s | 1 ms | 0.0% | 60% | 1314 MiB |
| 4 | 1 | 145 kb/s | 137 kb/s | 152 kb/s | 1 ms | 0.0% | 107% | 1553 MiB |
| 6 | 1 | 220 kb/s | 239 kb/s | 254 kb/s | 3 ms | 0.0% | 217% | 2078 MiB |
| 6 | 6 | 320 kb/s | — | — | 2 ms | 0.0% | 173% | 2135 MiB |

Every peer added costs a speaker another **43 kb/s** of upload (least-squares slope across 4 room sizes). A listener pays it too — `usedtx=0` keeps packets flowing while muted.

## Noise suppression cost

`rnnoise` is the desktop default, so its cost is one the product pays by default. Same room size and same speakers in every row — only the mode changes.

| Mode | Peers | Speaker ↑ | Room CPU | Room RSS | JS heap p50 |
|---|---|---|---|---|---|
| `off` | 3 | 102 kb/s | 53% | 1375 MiB | 4 MiB |
| `browser` | 3 | 102 kb/s | 60% | 1372 MiB | 4 MiB |
| `rnnoise` (default) | 3 | 102 kb/s | 73% | 1397 MiB | 4 MiB |

## Join latency

Clock starts at the join, not at browser launch — a real user already has a browser open.

| Room size | Reps | Signaling open | First audio heard |
|---|---|---|---|
| 3 | 3 | 36 ms | 246 ms |

## Host migration

The outage a mesh design owns, and one no SFU product has to survive. Host loss is detected by heartbeat timeout (~7 s), so that floor is in both columns.

| Room size | Survivors | New host elected | Audio restored |
|---|---|---|---|
| 4 | 3 | 7099 ms | 7917 ms |

## Video mesh scaling

Camera is a **full mesh** under the `p2p-only` preference this harness pins, so a sharer encodes and uploads one stream per other peer — the same `O(N-1)` shape as audio, at roughly twenty times the bitrate. Above two participants the shipped default (`allow-sfu`) may route this through Cloudflare Realtime instead, which is the topology **not** measured here. See `docs/video-routing.md`.

| Peers | Cameras | Sharer ↑ | Receiver ↓ | Picture sent | Room CPU | Room RSS |
|---|---|---|---|---|---|---|
| 2 | all (2) | 632 kb/s | 625 kb/s | 960×540 @ 30 fps (limited by bandwidth) | 93% | 1399 MiB |
| 3 | all (3) | 1.01 Mb/s | 1.01 Mb/s | 853×480 @ 16 fps | 223% | 1799 MiB |
| 4 | 1 | 1.90 Mb/s | 631 kb/s | 853×480 @ 30 fps | 200% | 1857 MiB |
| 4 | all (4) | 1.75 Mb/s | 1.59 Mb/s | 853×480 @ 4 fps (limited by bandwidth) | 337% | 2270 MiB |

Every peer added costs a camera sharer another **561 kb/s** of upload (least-squares slope across 3 room sizes). `videoScaleForPeerCount()` blunts the curve by halving the sent resolution above two peers and quartering it above four — the *Picture sent* column is where that shows.

_"Limited by bandwidth" here is the **app's own ceiling**, not a congested link:_ there is nothing to congest on loopback. `CAMERA_MAX_BITRATE` is 600 kb/s per listener, and an encoder that cannot fit the picture into its budget spends the budget on motion rather than on pixels. That is the intended trade.

> **This machine ran out of room.** A video run reached 337% of one core on a 4-core host — every peer of the room encoding at once. When that happens the frame rate falls first, and the encoder still reports its own bitrate cap as the reason, so read the frame rates above as the ceiling of the harness rather than of the app, and shrink `BENCH_VIDEO_SIZES` before quoting them.

## Camera background cost

Segmentation runs locally — MediaPipe in WebAssembly plus a WebGL composite — so the cost lands on **CPU and frame rate**, never on the wire. Same room size and the same cameras in every row; only the background changes.

| Background | Peers | Cameras | Still running | Sharer ↑ | Picture sent | Room CPU | Room RSS |
|---|---|---|---|---|---|---|---|
| `off` (default) | 2 | 1 | 0/1 | 611 kb/s | 960×540 @ 30 fps (limited by bandwidth) | 80% | 1319 MiB |
| `blur` | 2 | 1 | 0/1 | 627 kb/s | 1280×720 @ 30 fps | 160% | 1825 MiB |

_Still running_ is how many of the sharing peers had the effect on at the end of the run. It is recorded rather than assumed because the app drops the effect in two situations, both silent on the wire: `maybeApplyVideoEffects()` falls back to the raw camera when the segmentation runtime cannot start, and `VideoEffects.onOverload` turns the background off on a device that cannot sustain it.

> **The effect gave up on this machine.** At least one run above ended with the background off after the app decided the device could not keep up — which is real behaviour worth knowing about, but it means that row is **not** the cost of the effect. Measure the background on a machine that is not also running every other peer of the room.

## Screen share

A screen is not a camera: `SCREEN_MAX_BITRATE` is 1.5 Mb/s against the camera’s 600 kb/s, the sender is pinned to `maintain-resolution` (drop frames, never letters), and the peer-count downscale never applies to it.

| Peers | Sharer ↑ | Watcher ↓ | Picture sent | Room CPU | Room RSS |
|---|---|---|---|---|---|
| 3 | 920 kb/s | 460 kb/s | 1280×720 @ 20 fps | 130% | 1558 MiB |

## Video join latency

Walking into a meeting that is already on camera. Hearing the room and *seeing* it are two different numbers: a picture needs its own call per sharer, a keyframe and a decoder.

| Room size | Reps | Signaling open | First audio heard | Every camera visible |
|---|---|---|---|---|
| 3 | 3 | 41 ms | 322 ms | 586 ms |

<!-- /bench-results -->

## Reading the mesh honestly

Audio is a full mesh, always. A speaker uploads one Opus stream **per other
peer**, so upload grows as `O(N-1)` while an SFU-based product's stays flat —
and because `usedtx=0` keeps the receiver's jitter buffer warm, a *listener*
pays nearly the same. Everyone in a Voxal room pays for the mesh, not only
whoever is talking.

Camera and screen-share are the same shape under `p2p-only`, at roughly twenty
times the bitrate — which is exactly why the shipped default lets them take an
SFU above two participants while audio never does. The video figures above are
the **mesh** side of that choice; the relayed side is not measured here.

That is a real cost, and the tables print it plainly. The trade it buys is also
real:

- **Wins** — latency, privacy, join time, small-room CPU, and surviving the loss
  of the host at all.
- **Loses** — upload at high N, and any uplink-constrained network.

Pick the room size the product is actually for, and quote that one.
